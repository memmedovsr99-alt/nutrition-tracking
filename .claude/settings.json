{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"/Users/samir/Documents/Claude/Projects/Nutrition tracking\" && git add -A && git diff --cached --quiet || (git commit -m \"Auto-update $(date '+%Y-%m-%d %H:%M')\" && git push)"
          }
        ]
      }
    ]
  }
}
